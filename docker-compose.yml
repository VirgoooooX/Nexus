version: '3.8'
services:
  nexus:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - nexus-data:/app/prisma
    env_file:
      - .env.local
    restart: unless-stopped
volumes:
  nexus-data:
